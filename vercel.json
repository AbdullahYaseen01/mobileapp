{
  "buildCommand": "bash vercel-build.sh || flutter build web --release",
  "outputDirectory": "build/web",
  "installCommand": "echo 'Flutter dependencies handled by GitHub Actions'",
  "framework": null,
  "github": {
    "enabled": true,
    "autoAlias": true
  }
}

