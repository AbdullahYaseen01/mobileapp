name: study_planner
description: A comprehensive Study Planner app with Firebase backend
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.3
  cloud_firestore: ^4.13.6
  firebase_messaging: ^14.7.10

  # State Management
  provider: ^6.1.1

  # UI Components
  table_calendar: ^3.0.9
  flutter_local_notifications: ^16.3.0
  intl: ^0.19.0
  timezone: ^0.9.2

  # Utilities
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Assets (we'll add these later)
  # assets:
  #   - images/

